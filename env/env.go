package main

import (
  "os"
  "fmt"
)

func main() {
  // Obtain the entire environment as a slice
  env := ...
  // Iterate over env to output
  //   #     varname=value
  for ... {
    fmt.Printf("%-5d %s\n", i, ...)
  }
}
